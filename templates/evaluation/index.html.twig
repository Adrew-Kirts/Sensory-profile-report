{% extends 'base.html.twig' %}

{% block title %}Nouvelle evaluation{% endblock %}

{% block body %}
    <h1 class="text-xl">Profil sensoriel par tranche d'âge</h1>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4 p-4">
        <div class="card border-2 border-accent">
            <div class="card-body">
                <h2 class="card-title text-nowrap">Nourrisson (0-6M)</h2>
                <a href="{{ path('question.indexByAge', {'ageCategory': 0}) }}" class="btn btn-sm btn-secondary">Accéder</a>
            </div>
        </div>
        <div class="card border-2 border-accent">
            <div class="card-body">
                <h2 class="card-title">Jeune enfant (7-35M)</h2>
                <a href="{{ path('question.indexByAge', {'ageCategory': 1}) }}" class="btn btn-sm btn-secondary">Accéder</a>
            </div>
        </div>
        <div class="card border-2 border-accent">
            <div class="card-body">
                <h2 class="card-title">Enfant (3-14A)</h2>
                <a href="{{ path('question.indexByAge', {'ageCategory': 2}) }}" class="btn btn-sm btn-secondary">Accéder</a>
            </div>
        </div>
        <div class="card border-2 border-accent">
            <div class="card-body">
                <h2 class="card-title whitespace-normal">Adulte/Ado (11-65+ ans)</h2>
                <a href="{{ path('question.indexByAge', {'ageCategory': 3}) }}" class="btn btn-sm btn-secondary">Accéder</a>
            </div>
        </div>
{#        //TODO add this block#}
{#        <div class="card border-2 border-accent">#}
{#            <div class="card-body">#}
{#                <h2 class="card-title whitespace-nowrap">Compagnon scolaire (3-14A)</h2>#}
{#                <a href="{{ path('question.indexByAge', {'ageCategory': 4}) }}" class="btn btn-sm btn-secondary">Accéder</a>#}
{#            </div>#}
{#        </div>#}
{#    </div>#}
        <div class="card border-2 border-accent opacity-50 cursor-not-allowed">
            <div class="card-body">
                <h2 class="card-title whitespace-nowrap">Compagnon scolaire (3-14A)</h2>
                {# Removed href to make link non-clickable #}
                <a class="btn btn-sm btn-secondary pointer-events-none">Bientôt</a>
            </div>
        </div>
{% endblock %}
